{
    "CORSRules": [
        {
            // Only allow requests from your GitHub Pages domain
            "AllowedOrigins": [
                "https://alteredlife94660.github.io"
            ],
            // Specify which HTTP methods are allowed
            "AllowedMethods": [
                "GET",
                "HEAD"
            ],
            // Which headers can be used in the request
            "AllowedHeaders": [
                "*"
            ],
            // How long the browser should cache the CORS response
            "MaxAgeSeconds": 3000,
            // Which headers can be exposed to the browser
            "ExposeHeaders": [
                "ETag"
            ]
        }
    ]
}
